{
  "name": "synctabs-dev-server",
  "version": "1.0.0",
  "private": true,
  "description": "SyncTabs development server for local browser-to-browser sync testing",
  "main": "server.js",
  "bin": "server.js",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --verbose",
    "check": "node --check server.js"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
